PORT=4000
FRONTEND_ORIGIN=http://localhost:5173
# Database via SSH tunnel
PGHOST=127.0.0.1
PGPORT=5432
PGDATABASE=sales_warehouse
PGUSER=
PGPASSWORD=

# Optional: column used by /api/analytics/orders/raw date filters
# Set this to your table's date column. If it contains spaces or caps, just write it as-is (we quote it safely).
# Examples: order_date, created_at, Order Date
RAW_DATE_COLUMN=Order Date
