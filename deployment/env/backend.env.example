# --- Backend container environment ---
PORT=4000
NODE_ENV=production
FRONTEND_ORIGIN=http://frontend:8080
SESSION_SECRET=replace-with-long-random-value

# Optional PostgreSQL wiring (leave blank to keep mock JSON store)
PGHOST=
PGPORT=5432
PGDATABASE=
PGUSER=
PGPASSWORD=

# Optional analytics raw-data override
RAW_DATE_COLUMN=Order Date
