Place your brand logo file in this folder as `logo.png` (recommended 64–128px, square).

You can also point to a custom URL by setting `VITE_BRAND_LOGO_URL` in `frontend/.env.local`.

TopNav will fall back to the green "SD" badge if the image is missing.
